{
  "scripts": {
    "build": "esbuild --minify --bundle --outfile=../static/bundle.js src/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.8.16"
  },
  "dependencies": {
    "dat.gui": "^0.7.7",
    "socket.io-client": "^3.0.3",
    "three": "^0.123.0"
  }
}
