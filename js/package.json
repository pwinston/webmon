{
  "scripts": {
    "build": "esbuild --minify --splitting --bundle --format=esm --outdir=../static src/viewer.js"
  },
  "devDependencies": {
    "esbuild": "^0.8.16"
  },
  "dependencies": {
    "dat.gui": "^0.7.7",
    "socket.io-client": "^3.0.3",
    "three": "^0.123.0"
  }
}
